<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme (夜間模式). -->
    <style name="Theme.RehabilitationApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- ✅ Primary brand color -->
        <item name="colorPrimary">@color/brand_primary_dark</item>             <!-- 深湖綠作為主色 -->
        <item name="colorPrimaryVariant">@color/brand_primary</item>           <!-- 稍亮湖綠作為變體 -->
        <item name="colorOnPrimary">@color/text_invert</item>                  <!-- 主色上的文字 -->

        <!-- ✅ Secondary brand color -->
        <item name="colorSecondary">@color/brand_primary</item>                <!-- 一樣使用主湖綠 -->
        <item name="colorSecondaryVariant">@color/brand_primary_light</item>   <!-- 較淺作為次變體 -->
        <item name="colorOnSecondary">@color/text_invert</item>

        <!-- ✅ Status bar color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- ✅ Background color (夜間主背景為深底) -->
        <item name="android:windowBackground">@color/surface_dark</item>

    </style>
</resources>
